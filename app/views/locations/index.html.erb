<table>
  <thead>
    <tr>
      <th><h1>Locations</h1></th>
    </tr>
  </thead>

	<tbody>
	 	<% @locations.each do |location| %>
	 	<tr>
	 		<td><h3><%= location.name %></h3></td>
	 		<td><img height="50px" width="50px" src="<%= location.picture_url %>"></img></td>
	 		<td></td>
	 		<td><%= link_to "Show", location %></td>
	 		<td><%= link_to "Edit", edit_location_path(location) %></td>
	 		<td><%= link_to "Delete", location, method: :delete, data: {confirm:("Are you sure?") } %></td>
	 		<td></td>
	 		<td><%= location.snippet %></td>
	 	</tr>
	 	<% end %>
	</tbody>
<table>