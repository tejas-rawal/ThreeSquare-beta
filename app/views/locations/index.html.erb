<table>
  <thead>
    <tr>
      <th>Locations</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

	<tbody>
	 	<% @locations.each do |location| %>
	 	<tr>
	 		<td><%= location.name %></td>
	 		<td></td>
	 		<td></td>
	 		<td><%= link_to "Show", location_path(location.id) %></td>
	 		<td><%= link_to "Edit", edit_location_path(location.id) %></td>
	 		<td><%= link_to "Delete", location, method: :delete, data: {confirm:("Are you sure?") } %></td>
	 	</tr>
	 	<tr><%= location.snippet %></tr>
	 	<% end %>
	</tbody>
<table>

<p><%= link_to "Add New", new_location_post %></p>